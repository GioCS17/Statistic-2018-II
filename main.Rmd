---
title: "R Notebook"
output: html_notebook
---

```{r}

#Descargar
#juan estuvo aqui mas tiempo
install.packages("dplyr",dependencies = T)
library("dplyr")

a<-dat %>%
  group_by(anio) %>% 
  summarise(confirmados=sum(confirmados,na.rm = T),probables=sum(probables, na.rm = T))
a

#plot(dat$age,dat$Result_of_Treatment)

```
